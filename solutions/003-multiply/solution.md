# 解答: multiply（日本語）

この解答では `multiply` という JSON-RPC メソッドを提供します。位置引数として配列を受け取るか、オブジェクトの `values` キーに配列を入れた呼び出しを受け付けます。各要素が数値であることを検証し、積を返します。通知（`id` がないリクエスト）はレスポンスを返しません。パースエラーや不正なリクエストは JSON-RPC 2.0 の仕様に従って扱います。

実装例は `code/perl/app.psgi` と `code/python/server.py` にあります。
